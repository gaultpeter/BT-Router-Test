to run:

open cmd from this folder

mkdir classes

java javac -d classes -cp classes  src/btroutercheck/*.java

java -cp classes btroutercheck.ApplicationMain sample.csv

If adding a new csv file, please replace sample.csv with the name of the file. Also add the file to this folder. 